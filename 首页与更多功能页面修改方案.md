# 首页与更多功能页面修改方案

## 一、修改范围

本次修改主要涉及以下文件：

```text
pages/index/index.vue
pages/all-features/all-features.vue
pages.json
config/modules.js
components/home/
components/features/
```

其中：

- `pages/index/index.vue`：重构首页
- `pages/all-features/all-features.vue`：新增全部功能页面
- `pages.json`：注册页面和补充 TabBar 文案
- `config/modules.js`：统一管理所有功能入口
- `components/home/`：首页组件
- `components/features/`：全部功能页组件

---

# 二、修改目标

当前首页存在的问题：

- 首屏标题和免责声明占用空间过大
- 所有功能入口权重相同
- 用户无法快速找到最近使用和常用功能
- 会员状态和剩余试用次数没有展示
- 功能入口持续增加后首页会越来越长
- 模块跳转和会员校验代码重复
- 图标、文案和功能命名不统一
- 部分无效入口可能导致页面跳转失败

修改后的目标：

```text
用户进入首页后 3 秒内知道：
1. 自己的会员状态
2. 当前剩余使用次数
3. 上次编辑的内容
4. 最常用功能在哪里
5. 全部功能在哪里查找
```

---

# 三、首页修改方案

## 3.1 删除当前大标题区域

当前：

```text
功能管理中心
专业的聊天记录生成工具
```

建议删除。

改为顶部用户会员卡。

---

## 3.2 新增顶部用户会员卡

页面顶部展示：

```text
用户头像
用户昵称
会员状态
会员到期时间
剩余试用次数
会员中心入口
```

示例：

```text
你好，赵二狗

永久会员
会员有效期：永久有效

今日可用次数：∞
会员中心 >
```

普通用户：

```text
你好，赵二狗

普通用户
剩余试用：3 次

开通会员 >
```

建议计算属性：

```js
computed: {
  isMemberValid() {
    return !isMemberExpired(this.guestInfo.memberExpireAt)
  },

  memberText() {
    return this.isMemberValid ? '会员用户' : '普通用户'
  },

  usageText() {
    if (this.isMemberValid) {
      return '不限次数'
    }

    return `剩余 ${Number(this.guestInfo.tryCount || 0)} 次`
  }
}
```

---

## 3.3 压缩公告区域

当前免责声明占据整行黄色区域。

建议改成单行公告：

```text
📢 内容仅用于演示、创作和教学用途      >
```

要求：

- 高度控制在 `72～88rpx`
- 支持点击查看完整说明
- 支持关闭
- 关闭后本地记录
- 首页不重复展示其他免责声明

本地记录：

```js
uni.setStorageSync('homeAnnouncementClosed', true)
```

---

## 3.4 新增两个核心入口

首页首屏重点展示两个入口。

### 导入图片识别

```text
导入图片识别
自动提取金额、时间和订单信息
立即导入
```

点击进入图片识别流程。

### 直接选择模板

```text
直接选择模板
无需图片，选择模板后手动编辑
选择模板
```

点击进入模板库。

推荐手机端上下排列，不建议两张卡片并排。

---

## 3.5 新增继续编辑区域

存在草稿时展示：

```text
继续编辑

威信付款模板
转账-给为理想而奋斗
-0.012

修改于 5 分钟前

继续编辑 >
```

没有草稿时隐藏整个区域。

草稿数据建议：

```js
{
  id: '123',
  templateType: 'wechat-transfer',
  title: '威信付款模板',
  name: '给为理想而奋斗',
  money: '-0.012',
  previewImage: '',
  updateTime: 1720000000000,
  status: 'draft'
}
```

首页只展示最近一条草稿。

---

## 3.6 新增常用功能区域

首页固定展示 6 个常用入口：

```text
威信聊天
威信付款模板
企业群聊
个人群聊
转账记录
更多功能
```

布局：

```text
两行三列
```

每个入口只展示：

- 图标
- 功能名称
- 可选角标

不要展示长副标题。

角标：

```text
热门
新
会员
草稿
```

---

## 3.7 新增热门模板区域

首页展示 4～6 个热门模板。

采用横向滚动：

```text
转账付款
扫码付款
扫码收款
商户付款
小程序付款
```

模板卡片包含：

- 缩略图
- 模板名称
- 一行说明
- 使用模板按钮

右上角增加：

```text
全部模板 >
```

---

## 3.8 新增全部功能分类入口

首页底部显示：

```text
常用
聊天
群聊
付款
企业
更多
```

点击后进入全部功能页面，并传递分类参数：

```js
goAllFeatures(category = 'all') {
  uni.navigateTo({
    url: `/pages/all-features/all-features?category=${category}`
  })
}
```

---

# 四、全部功能页面修改方案

## 4.1 新增页面

新增：

```text
pages/all-features/all-features.vue
```

页面标题：

```text
全部功能
```

---

## 4.2 页面结构

```text
顶部导航

搜索功能名称

全部 常用 聊天 群聊 付款 企业 支付宝 工具

最近使用

常用功能

聊天记录

群聊制作

付款模板

企业功能

支付宝

更多工具
```

---

## 4.3 新增搜索功能

搜索范围：

- 功能名称
- 功能副标题
- 关键词
- 别名

搜索代码：

```js
computed: {
  filteredModules() {
    const keyword = this.searchKeyword.trim().toLowerCase()

    let list = this.modules.filter(item => item.enabled)

    if (this.activeCategory !== 'all') {
      list = list.filter(item => item.category === this.activeCategory)
    }

    if (!keyword) {
      return list
    }

    return list.filter(item => {
      const text = [
        item.title,
        item.subtitle,
        ...(item.keywords || [])
      ].join(' ').toLowerCase()

      return text.includes(keyword)
    })
  }
}
```

---

## 4.4 分类导航

手机端使用顶部横向标签：

```text
全部
常用
聊天
群聊
付款
企业
支付宝
工具
```

不建议手机端使用长期固定左侧菜单。

平板和 H5 宽屏可增加左侧分类栏。

---

## 4.5 功能卡片

推荐两列卡片。

每张卡片展示：

- 功能图标
- 功能名称
- 一行副标题
- 收藏按钮
- 会员角标
- 热门或新功能角标

状态：

```text
普通
热门
新功能
会员专享
开发中
```

开发中的功能不出现在首页。

---

## 4.6 收藏功能

点击：

```text
☆ → ★
```

收藏后进入“常用”分类。

本地存储：

```js
const FAVORITE_KEY = 'favoriteModules'

saveFavorites(ids) {
  uni.setStorageSync(FAVORITE_KEY, ids)
}
```

---

## 4.7 最近使用

记录每个功能的使用时间和次数：

```js
{
  moduleId: 'wechat-payment-template',
  count: 8,
  lastUsedAt: 1720000000000
}
```

最多保留最近 8 个。

排序：

```text
最后使用时间倒序
```

---

# 五、模块配置化修改

## 5.1 新增配置文件

新增：

```text
config/modules.js
```

示例：

```js
export const MODULES = [
  {
    id: 'wechat-chat',
    category: 'chat',
    platform: 'wechat',
    title: '威信聊天',
    subtitle: '单聊对话制作',
    icon: '/static/modules/wechat-chat.png',
    route: '/pages/wxChatList/wxChatList',
    keywords: ['微信', '威信', '聊天', '单聊'],
    requiresMember: true,
    featured: true,
    enabled: true
  },

  {
    id: 'wechat-payment-template',
    category: 'payment',
    platform: 'wechat',
    title: '威信付款模板',
    subtitle: '付款页面制作',
    icon: '/static/modules/wechat-payment.png',
    route: '/pages/codePay/codePay',
    keywords: ['付款', '账单', '扫码', '转账'],
    requiresMember: true,
    featured: true,
    badge: '热门',
    enabled: true
  },

  {
    id: 'enterprise-chat',
    category: 'enterprise',
    platform: 'enterprise',
    title: '企业聊天',
    subtitle: '企业聊天演示制作',
    icon: '/static/modules/enterprise-chat.png',
    route: '/pages/chatList/chatList',
    keywords: ['企业', '聊天', '单聊'],
    requiresMember: true,
    featured: true,
    enabled: true
  },

  {
    id: 'all-features',
    category: 'system',
    platform: 'system',
    title: '更多功能',
    subtitle: '',
    icon: '/static/modules/more.png',
    route: '/pages/all-features/all-features',
    keywords: ['更多', '全部', '功能'],
    requiresMember: false,
    enabled: true
  }
]
```

---

## 5.2 删除 handler 跳转方式

当前：

```js
handler: 'gotoCodePay'
```

建议删除。

改成：

```js
route: '/pages/codePay/codePay'
```

点击时统一处理。

---

# 六、统一功能入口逻辑

## 6.1 统一点击方法

```js
async enterModule(module) {
  if (!module || !module.enabled) {
    uni.showToast({
      title: '功能暂未开放',
      icon: 'none'
    })
    return
  }

  if (module.requiresMember) {
    const allowed = await this.checkFeatureAccess()

    if (!allowed) return
  }

  this.recordModuleUsage(module.id)

  uni.navigateTo({
    url: module.route
  })
}
```

---

## 6.2 抽取会员判断

```js
async checkFeatureAccess() {
  if (!isMemberExpired(this.guestInfo.memberExpireAt)) {
    return true
  }

  const tryCount = Number(this.guestInfo.tryCount || 0)

  if (tryCount <= 0) {
    uni.showModal({
      title: '试用次数已用完',
      content: '开通会员后可继续使用全部功能',
      confirmText: '查看会员',
      success: (res) => {
        if (res.confirm) {
          uni.switchTab({
            url: '/pages/mine/mine'
          })
        }
      }
    })

    return false
  }

  try {
    await updateUseFeature(this.guestInfo.id)

    this.guestInfo.tryCount = tryCount - 1

    return true
  } catch (error) {
    uni.showToast({
      title: '使用次数扣减失败，请重试',
      icon: 'none'
    })

    return false
  }
}
```

所有功能入口统一调用，不再重复写会员判断。

---

# 七、页面路由修改

在 `pages.json` 中新增：

```json
{
  "path": "pages/all-features/all-features",
  "style": {
    "navigationBarTitleText": "全部功能",
    "enablePullDownRefresh": false,
    "navigationStyle": "custom"
  }
}
```

可选增加模板库：

```json
{
  "path": "pages/template-gallery/template-gallery",
  "style": {
    "navigationBarTitleText": "模板中心",
    "enablePullDownRefresh": false,
    "navigationStyle": "custom"
  }
}
```

---

# 八、TabBar 修改

当前 TabBar 没有文字。

建议改成：

```json
"tabBar": {
  "color": "#8A8F99",
  "selectedColor": "#4C4EE8",
  "borderStyle": "black",
  "backgroundColor": "#FFFFFF",
  "list": [
    {
      "pagePath": "pages/index/index",
      "text": "首页",
      "iconPath": "/static/icon-home.png",
      "selectedIconPath": "/static/icon-home-active.png"
    },
    {
      "pagePath": "pages/mine/mine",
      "text": "我的",
      "iconPath": "/static/icon-mine.png",
      "selectedIconPath": "/static/icon-mine-active.png"
    }
  ]
}
```

第一阶段只保留：

```text
首页
我的
```

---

# 九、首页组件拆分

建议拆成：

```text
pages/index/
├── index.vue
└── components/
    ├── HomeUserCard.vue
    ├── HomeAnnouncement.vue
    ├── HomePrimaryActions.vue
    ├── HomeContinueEditing.vue
    ├── HomeCommonModules.vue
    ├── HomePopularTemplates.vue
    └── HomeCategoryEntry.vue
```

职责：

### `HomeUserCard.vue`

负责：

- 用户头像
- 用户昵称
- 会员状态
- 剩余次数
- 会员中心入口

### `HomeAnnouncement.vue`

负责：

- 单行公告
- 关闭公告
- 查看完整说明

### `HomePrimaryActions.vue`

负责：

- 导入图片
- 选择模板

### `HomeContinueEditing.vue`

负责：

- 最近草稿
- 继续编辑

### `HomeCommonModules.vue`

负责：

- 六个常用功能
- 更多功能入口

### `HomePopularTemplates.vue`

负责：

- 热门模板横向列表

### `HomeCategoryEntry.vue`

负责：

- 分类快捷入口

---

# 十、全部功能页组件拆分

```text
pages/all-features/
├── all-features.vue
└── components/
    ├── FeatureSearchBar.vue
    ├── FeatureCategoryTabs.vue
    ├── FeatureCard.vue
    ├── FeatureSection.vue
    ├── RecentFeatureList.vue
    └── MemberPromotionCard.vue
```

---

# 十一、视觉修改规范

## 页面背景

```css
background: #F6F7F9;
```

不要整页使用高饱和紫色渐变。

---

## 卡片

```css
background: #FFFFFF;
border-radius: 24rpx;
box-shadow: 0 8rpx 30rpx rgba(31, 35, 48, 0.06);
```

---

## 页面间距

```text
左右边距：24～32rpx
卡片间距：20～24rpx
区块间距：28～36rpx
卡片内边距：24～32rpx
```

---

## 字体

```text
页面主标题：36～40rpx
区块标题：32～36rpx
卡片标题：28～32rpx
卡片副标题：22～26rpx
辅助文字：22～24rpx
```

删除当前 `72rpx` 大标题。

---

## 图标

统一使用：

```text
PNG
SVG
uni-icons
```

不要继续混合 Emoji 和多种风格图片。

---

## 推荐颜色

```text
主色：#4C4EE8
辅助蓝：#3478F6
成功绿：#25B864
提醒橙：#FF9F0A
危险红：#FF4D4F
页面背景：#F6F7F9
主要文字：#1F2329
辅助文字：#8A8F99
```

---

# 十二、文案修改

建议统一使用：

```text
剧情创作
演示素材
页面制作
模板生成
聊天演示
付款页面制作
```

删除或替换：

| 当前文案 | 修改文案 |
|---|---|
| 恶搞专用 | 剧情创作与聊天演示 |
| 快速建立信任背书 | 制作企业业务演示素材 |
| 以假乱真 | 还原付款页面视觉效果 |
| 快速生成 w-x | 快速制作聊天演示内容 |
| 功能管理中心 | 创作中心 |

---

# 十三、无效入口处理

检查以下入口是否已经在 `pages.json` 注册：

```text
/pages/wxCollectionBill/wxCollectionBill
/pages/previewGallery/previewGallery
```

处理规则：

- 页面存在：恢复注册
- 页面不存在：隐藏入口
- 不允许首页展示点击后报错的功能

---

# 十四、实施优先级

## P0

1. 新建 `config/modules.js`
2. 抽取统一 `enterModule()`
3. 抽取统一会员判断
4. 首页顶部改为会员卡
5. 压缩公告
6. 增加两个核心入口
7. 增加六个常用功能
8. 新建全部功能页面
9. TabBar 增加文字
10. 隐藏无效入口
11. 修改高风险文案

## P1

1. 增加继续编辑草稿
2. 增加热门模板
3. 增加搜索
4. 增加功能分类
5. 增加收藏
6. 增加最近使用
7. 统一图标

## P2

1. 根据使用频率动态排序
2. 云端同步收藏
3. 云端同步最近使用
4. 首页运营推荐位
5. 模板个性化推荐
6. 新功能角标配置

---

# 十五、建议开发顺序

```text
第 1 步：整理全部功能配置
第 2 步：统一跳转和会员校验
第 3 步：创建全部功能页面
第 4 步：重构首页顶部
第 5 步：增加两个核心入口
第 6 步：增加常用功能
第 7 步：增加搜索和分类
第 8 步：增加草稿和热门模板
第 9 步：统一图标和文案
```

---

# 十六、验收标准

## 首页验收

- 用户状态和会员状态正确显示
- 剩余使用次数正确
- 首页首屏能看到两个核心入口
- 常用功能最多展示 6 个
- 更多功能入口可正常打开
- 无效功能不显示
- 页面不再出现超大标题
- 联系客服不占据首页大区域
- TabBar 显示“首页”和“我的”

## 全部功能页验收

- 支持搜索
- 支持分类切换
- 支持收藏
- 支持最近使用
- 功能卡片可正确跳转
- 会员功能统一校验
- 开发中功能有明确状态
- 所有入口均有统一图标和名称

## 代码验收

- 不再大量存在 `goToXXX()` 重复方法
- 不再重复编写会员判断
- 新增功能只需要修改 `modules.js`
- 页面模块拆分清晰
- `pages.json` 中不存在无效路由
- 首页文案不再出现高风险表达

---

# 十七、最终页面原则

首页只展示：

```text
用户状态
两个核心动作
一条草稿
六个常用功能
少量热门模板
全部功能入口
```

全部功能页负责：

```text
搜索
分类
收藏
最近使用
完整功能列表
```

最终用户路径：

```text
首页快速开始
→ 需要其他功能时进入全部功能
→ 选择功能
→ 编辑
→ 预览
→ 保存
```

这套修改可以避免首页随着功能增加不断变长，也能让新增模块只改配置，不再持续修改首页结构和大量跳转代码。
