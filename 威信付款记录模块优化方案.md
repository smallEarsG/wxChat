# 威信付款记录模块优化方案

## 一、模块现状判断

当前“威信付款记录”模块已经具备较完整的功能，包括：

- 图片识别
- 固定模板选择
- 自定义模板
- 付款记录编辑
- 头像管理
- 修改记录
- 模板预览
- 云端账单保存

但目前的问题是：

> 功能已经很多，入口和操作路径却不够清晰。

用户第一次进入后，很难马上理解应该：

- 先上传图片
- 先选择模板
- 先创建自定义模板
- 还是先查看历史记录

当前模块更像多个功能页面的集合，还没有形成清晰的产品流程。

---

## 二、当前模块主要问题

### 1. 模块首页功能堆叠严重

当前首页同时展示：

- 在线识别
- 离线识别
- 使用指南
- 图片预览
- 识别结果
- 模板预览入口
- 创建自定义模板
- 自定义模板列表
- 十多个固定模板按钮
- 修改记录入口

用户需要滚动较长距离，才能看到完整内容。

问题不是功能少，而是没有明确的主次关系。

建议首页只突出两个核心入口：

```text
导入图片识别
直接选择模板
```

其他功能放到二级页面或次要入口。

---

### 2. 页面标题与实际功能不一致

当前页面标题是：

```text
图片文字识别
```

但这个页面实际上承担整个付款记录模块的首页职责。

它不仅包含图片识别，还包含：

- 模板选择
- 自定义模板
- 历史记录
- 编辑入口

建议将模块首页标题改为：

```text
付款模板
```

或者：

```text
付款演示模板
```

图片识别只是其中一个入口，不应该代表整个模块。

---

### 3. 首次引导过于强制

当前首次进入会弹出完整使用指南，并且需要等待倒计时结束才能关闭。

关闭后，页面正文又展示一遍类似的使用指南。

这种设计容易让用户产生以下感觉：

- 内容重复
- 阅读负担重
- 无法快速开始使用
- 被强制等待

建议取消强制倒计时。

首次引导可以改成三页轻量说明：

```text
第 1 页：导入图片或直接选择模板
第 2 页：确认识别出的金额、时间和单号
第 3 页：编辑完成后保存到记录
```

允许用户立即跳过。

页面正文只保留：

```text
不会使用？查看教程 >
```

---

### 4. 识别结果缺少确认步骤

当前图片识别完成后，用户只看到识别成功提示和识别到的文字行数。

但用户无法在选择模板前确认：

- 金额是否正确
- 时间是否正确
- 交易单号是否完整
- 商户单号是否正确
- 哪些字段没有识别到

建议增加识别结果确认页面：

```text
识别完成                         8 个字段

机构名称
[ 某某商户                    ]

金额
[ -100.00                     ]

支付时间
[ 2026-07-11 15:30:22         ]

支付方式
[ 零钱通                       ]

交易单号
[ 1000050001...                ]

商户单号
[ 20260711...                  ]

未识别字段：收款时间、备注

重新识别                  下一步选择模板
```

推荐流程：

```text
选择图片
→ 识别内容
→ 确认识别结果
→ 选择模板
→ 编辑和预览
→ 保存记录
```

这样用户可以在进入模板前修正数据，减少后续重复编辑。

---

### 5. 模板入口过多且缺少分类

当前固定模板直接平铺在首页，包括：

- 转账付款
- 扫码收款
- 第三方付款
- 第三方小程序
- 第三方付款（电话）
- 第三方条形码 16 位
- 第三方条形码 32 位
- 第三方条形码 34 位
- 扫码付款
- 小程序条形码
- 第三方商家

普通用户很难快速理解：

- 每个模板有什么区别
- 哪个模板适合自己
- 不同条形码位数有什么意义
- “第三方付款”和“第三方商家”有什么区别

建议改成分类模板库。

---

## 三、推荐的模块首页结构

建议首页改成：

```text
付款模板

┌────────────────────────────┐
│ 导入图片识别                │
│ 自动提取金额、时间和单号     │
└────────────────────────────┘

┌────────────────────────────┐
│ 直接选择模板                │
│ 无需图片，手动编辑内容       │
└────────────────────────────┘

最近使用
转账付款    扫码付款    第三方付款

模板分类
转账类  扫码类  商户类  自定义

我的模板
已有 3 个模板                 >

最近记录
查看已保存和未完成记录         >
```

首页只承担导航作用，不再直接堆放全部模板和全部配置。

---

## 四、模板分类建议

### 1. 转账类

```text
转账付款
转账收款
```

### 2. 扫码类

```text
扫码付款
扫码收款
```

### 3. 商户付款类

```text
普通商户付款
商户付款（带电话）
商家详情付款
```

### 4. 条形码和小程序类

```text
条形码账单
小程序账单
小程序条形码账单
```

不要在模板名称中直接突出：

```text
16 位
32 位
34 位
```

进入条形码模板后再让用户选择：

```text
条形码格式

自动匹配
16 位
32 位
34 位
```

默认使用：

```text
自动匹配
```

这样对普通用户更友好。

---

## 五、统一模板选择页面

建议新增统一模板库页面：

```text
pages/bill-template-gallery/bill-template-gallery.vue
```

模板卡片示例：

```text
┌──────────────────────┐
│       模板缩略图      │
│                      │
├──────────────────────┤
│ 转账付款              │
│ 适合转账详情演示       │
│              使用模板 │
└──────────────────────┘
```

模板卡片应包含：

- 模板缩略图
- 模板名称
- 简短说明
- 模板分类
- 最近使用标记
- 收藏按钮
- 使用模板按钮
- 自定义模板标识

页面支持：

- 搜索模板
- 分类筛选
- 最近使用
- 我的收藏
- 我的自定义模板

用户不应该仅根据按钮文字猜模板效果。

---

## 六、自定义模板管理优化

当前自定义模板直接展示在识别首页中，会让首页进一步变长。

建议拆成独立页面：

```text
我的模板
```

列表结构：

```text
自定义付款模板
更新于 2026-07-11 15:30

编辑    复制    使用    删除
```

建议增加：

- 模板复制
- 模板重命名
- 模板预览
- 按更新时间排序
- 搜索模板
- 删除二次确认
- 设置默认模板
- 最近使用标识

自定义模板和固定模板统一进入同一个模板库，不要分散在多个入口中。

---

## 七、编辑页面交互优化

目前用户主要通过：

- 点击头像更换头像
- 点击金额打开编辑表单

这种隐藏式操作不够明显。

新用户可能不知道金额和头像可以点击。

建议在编辑页面底部增加固定工具栏：

```text
编辑信息   更换头像   样式   预览   保存
```

### 1. 编辑信息

打开分组表单：

```text
基础信息
机构名称
金额
当前状态

时间信息
付款时间
收款时间

订单信息
支付方式
交易单号
商户单号

备注信息
付款说明
收款方备注
商户备注

页面设置
字体大小
左右边距
是否显示全部账单
```

不要把所有字段一次性堆到一个无分组长表单中。

### 2. 更换头像

点击后打开统一头像选择器：

```text
最近使用
我的头像
新增头像
```

支持：

- 搜索头像
- 头像名称
- 删除确认
- 最近使用
- 默认头像

### 3. 样式

集中管理：

- 字体大小
- 左右边距
- 背景样式
- 是否显示服务模块
- 是否显示条形码
- 是否显示全部账单
- 页面缩放比例

### 4. 预览

提供：

- 全屏预览
- 长图预览
- 隐藏编辑按钮
- 模拟真实页面显示
- 返回编辑

### 5. 保存

明确显示保存状态：

```text
未保存
保存中…
已保存
保存失败
```

---

## 八、保存机制优化

### 当前问题

当前用户执行以下操作后，页面可能立即调用保存接口：

- 更换头像
- 提交表单
- 修改信息
- 进入识别后的模板

这会产生：

- 频繁请求后端
- 半成品记录
- 用户误操作也创建记录
- 网络失败后状态不明确
- 修改记录中出现大量无意义数据

### 推荐方案：草稿机制

流程改成：

```text
进入编辑页
→ 本地编辑
→ 防抖保存草稿
→ 用户点击完成
→ 标记为正式记录
```

增加记录状态：

```text
draft
completed
```

数据结构示例：

```js
{
  id: 'xxx',
  status: 'draft',
  templateType: 'transfer',
  billDetail: {},
  updateTime: 1720000000000
}
```

### 防抖保存

```js
scheduleSave() {
  clearTimeout(this.saveTimer)

  this.saveTimer = setTimeout(() => {
    this.saveBill()
  }, 1000)
}
```

用户连续修改时，只在停止操作后保存一次。

### 显式完成

底部按钮：

```text
保存草稿
完成制作
```

点击“完成制作”后，记录状态改为：

```text
completed
```

---

## 九、修改记录页面优化

### 1. 删除硬编码测试数据

当前记录页面不应该包含大量 Base64 图片或大段测试数据。

问题包括：

- 文件体积变大
- 页面初始化变慢
- 内存占用增加
- Git Diff 难以阅读
- 打包产物膨胀

测试数据应放到独立 Mock 文件，生产环境不加载。

### 2. 修正空状态文案

当前空状态如果显示：

```text
暂无消息
```

语义不准确。

建议改成：

```text
暂无付款模板记录
创建第一条记录 >
```

### 3. 记录列表结构

```text
今天

转账付款
某某商户 · -100.00
15:32                       编辑 >

扫码收款
某某用户 · +300.00
14:10                       编辑 >
```

### 4. 增加记录筛选

支持：

- 全部
- 草稿
- 已完成
- 转账付款
- 扫码付款
- 商户付款
- 自定义模板

### 5. 增加记录搜索

可搜索：

- 名称
- 金额
- 单号
- 备注
- 模板名称

### 6. 记录操作

每条记录支持：

```text
继续编辑
复制记录
使用相同模板新建
重命名
删除
```

删除必须二次确认。

---

## 十、统一账单类型定义

当前账单类型使用数字表示，例如：

```text
1 = 转账付款
2 = 扫码收款
3 = 扫码付款
4 = 第三方付款
```

如果不同页面各自写数字，后续容易出现映射错误。

建议建立统一枚举：

```js
export const BILL_TYPE = Object.freeze({
  TRANSFER: 1,
  QR_RECEIVE: 2,
  QR_PAY: 3,
  THIRD_PARTY: 4,
  MINI_PROGRAM: 5,
  BARCODE_16: 6,
  BARCODE_32: 7,
  BARCODE_34: 8,
  MINI_PROGRAM_BARCODE: 9,
  THIRD_PARTY_MERCHANT: 10,
  CUSTOM: 99
})
```

使用方式：

```js
billType: BILL_TYPE.TRANSFER
```

不要继续使用：

```js
billType: 1
```

同时更新 API 注释和后端文档，保证定义一致。

---

## 十一、统一模板路由配置

当前模板跳转依赖数字索引和路由映射：

```js
const routeMap = {
  0: '/pages/transfer/transfer',
  1: '/pages/codePayChild/codePayChild',
  2: '/pages/ThirdpartyPayment/ThirdpartyPayment'
}
```

这种方式存在问题：

- 数字没有语义
- 修改顺序容易出错
- 新增模板需要修改多个位置
- 前后端账单类型容易映射错

建议改成：

```js
export const BILL_TEMPLATE_MAP = {
  transfer: {
    route: '/pages/bill-editor/bill-editor',
    billType: BILL_TYPE.TRANSFER,
    title: '转账付款',
    previewComponent: 'TransferBill'
  },

  qrReceive: {
    route: '/pages/bill-editor/bill-editor',
    billType: BILL_TYPE.QR_RECEIVE,
    title: '扫码收款',
    previewComponent: 'CodePayChildBill'
  },

  qrPay: {
    route: '/pages/bill-editor/bill-editor',
    billType: BILL_TYPE.QR_PAY,
    title: '扫码付款',
    previewComponent: 'CodePayChild2Bill'
  }
}
```

路由：

```text
/pages/bill-editor/bill-editor?template=transfer
/pages/bill-editor/bill-editor?template=qrReceive
/pages/bill-editor/bill-editor?template=qrPay
```

---

## 十二、合并重复编辑页面

当前多个页面重复实现：

- 加载账单
- 创建账单
- 更新账单
- 加载头像
- 上传头像
- 删除头像
- 打开编辑弹窗
- 返回页面
- 处理路由参数

例如：

```text
pages/transfer/transfer.vue
pages/codePayChild/codePayChild.vue
pages/codePayChild2/codePayChild2.vue
pages/ThirdpartyPayment/ThirdpartyPayment.vue
```

建议合并成统一编辑器：

```text
pages/bill-editor/bill-editor.vue
```

统一编辑器负责：

- 路由参数解析
- 默认数据加载
- 云端账单查询
- 草稿创建
- 自动保存
- 头像管理
- 编辑弹窗
- 底部工具栏
- 预览组件切换

不同模板只保留预览组件：

```text
components/bill-preview/
├── TransferBill.vue
├── CodePayChildBill.vue
├── CodePayChild2Bill.vue
├── ThirdpartyPaymentBill.vue
└── CustomBill.vue
```

---

## 十三、抽取统一逻辑

建议抽取以下模块。

### 1. 账单编辑 Mixin 或 Composable

```text
mixins/billEditorMixin.js
```

负责：

- 加载账单
- 新建账单
- 更新账单
- 保存状态
- 防抖保存
- 草稿状态
- 返回提醒

### 2. 头像管理

```text
mixins/avatarManagerMixin.js
```

负责：

- 加载头像
- 上传头像
- 创建头像
- 删除头像
- 选择头像

### 3. 模板默认数据

```text
config/billTemplates.js
```

负责：

- 模板名称
- 默认字段
- 默认数据
- 预览组件
- 账单类型
- 分类
- 模板描述

### 4. 账单字段定义

```text
config/billFields.js
```

统一字段：

```js
{
  key: 'money',
  label: '金额',
  type: 'money',
  required: true
}
```

---

## 十四、模板预览入口处理

当前如果存在“模板预览模式”入口，需要保证：

- 页面文件真实存在
- 已在 `pages.json` 注册
- 路由名称一致
- 入口有明确用途

推荐将模板预览统一放入模板库页面中，不再单独放一个模糊入口。

模板库中直接展示缩略图，点击后进入全屏预览。

---

## 十五、文案和命名统一

当前模块中存在较多不统一命名，例如：

```text
转账付款
扫码收款
扫码付款
第三方付款
第三方商家
第三方小程序
```

建议统一命名规则：

```text
动作 + 场景
```

例如：

```text
转账付款
扫码付款
扫码收款
商户付款
小程序付款
条形码付款
```

同时修正错别字：

```text
条形玛
```

改为：

```text
条形码
```

页面、路由和组件命名也应统一：

```text
codePay
codePayChild
ThirdpartyPayment
```

逐步改为：

```text
billHome
billEditor
thirdPartyPaymentBill
```

---

## 十六、推荐的完整用户流程

```text
付款模板首页
│
├─ 导入图片识别
│    ├─ 选择图片
│    ├─ 识别内容
│    ├─ 确认识别字段
│    └─ 选择模板
│
├─ 直接选择模板
│    ├─ 模板分类
│    ├─ 模板缩略图
│    └─ 使用模板
│
├─ 我的模板
│    ├─ 新建模板
│    ├─ 编辑模板
│    ├─ 复制模板
│    └─ 删除模板
│
└─ 最近记录
     ├─ 草稿
     ├─ 已完成
     └─ 继续编辑

进入统一编辑器
│
├─ 编辑信息
├─ 更换头像
├─ 页面样式
├─ 全屏预览
├─ 保存草稿
└─ 完成制作
```

---

## 十七、建议页面结构

```text
pages/
├── bill-home/
│   └── bill-home.vue
│
├── bill-template-gallery/
│   └── bill-template-gallery.vue
│
├── bill-recognition-result/
│   └── bill-recognition-result.vue
│
├── bill-editor/
│   └── bill-editor.vue
│
├── bill-records/
│   └── bill-records.vue
│
├── template-config/
│   └── template-config.vue
│
└── custom-templates/
    └── custom-templates.vue
```

组件：

```text
components/
├── bill-preview/
├── bill-editor/
│   ├── BillBottomToolbar.vue
│   ├── BillInfoEditor.vue
│   ├── BillStyleEditor.vue
│   ├── BillAvatarPicker.vue
│   └── BillSaveStatus.vue
│
└── bill-template/
    ├── BillTemplateCard.vue
    ├── BillTemplateCategory.vue
    └── BillTemplateSearch.vue
```

---

## 十八、实施优先级

### P0：先修可用性问题

1. 首页只保留“导入图片”和“选择模板”两个主入口
2. 增加识别结果确认页
3. 十多个模板改成分类模板库
4. 修复或删除无效的模板预览路由
5. 删除记录页硬编码测试数据
6. 修正错别字和页面标题
7. 首次引导取消强制倒计时

### P1：编辑体验改造

1. 编辑页增加底部工具栏
2. 编辑表单按字段分组
3. 增加保存状态
4. 自动保存增加防抖
5. 增加草稿和已完成状态
6. 历史记录增加搜索和分类
7. 自定义模板移到独立页面

### P2：代码重构

1. 合并重复账单编辑页面
2. 建立统一账单编辑器
3. 抽取头像管理逻辑
4. 抽取账单保存逻辑
5. 建立统一 `BILL_TYPE`
6. 建立模板配置表
7. 统一页面、组件和路由命名

---

## 十九、第一阶段最低改造方案

如果暂时不进行大规模重构，第一阶段可以只完成以下内容。

### 1. 首页改版

保留：

```text
导入图片识别
直接选择模板
我的模板
最近记录
```

删除首页中的全部模板按钮。

### 2. 新增模板库页面

将原来的模板按钮移动到模板库，并增加分类。

### 3. 增加结果确认页面

识别后先展示字段表单，确认后再选择模板。

### 4. 增加底部工具栏

```text
编辑   头像   样式   预览   保存
```

### 5. 增加保存状态

```text
未保存
保存中
已保存
保存失败
```

### 6. 删除硬编码记录

记录列表只通过接口获取数据。

这六项完成后，模块体验会有明显改善，同时不会要求立即重写所有模板页面。

---

## 二十、最终建议

这个模块不建议继续通过增加按钮和新增独立页面扩展。

推荐方向是：

> **首页负责导航，模板库负责选择，统一编辑器负责制作，记录页负责管理。**

最值得优先完成的三项：

1. 把十多个模板按钮改成分类模板库
2. 增加识别结果确认页面
3. 给编辑页面增加统一底部工具栏

完成这三项后，用户的使用路径会从“看见一堆按钮自己猜”，变成：

```text
选择数据来源
→ 选择模板
→ 编辑内容
→ 预览
→ 保存
```

这才是一个完整、容易理解的付款模板模块。
